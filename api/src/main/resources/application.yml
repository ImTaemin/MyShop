spring:
  profiles:
    active: prod, dev
    include: jwt, gcp, pay

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


  cloud:
    gcp:
      credentials:
        location: classpath:myshop-373122-5a778e754845.json

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 100MB
      file-size-threshold: 2KB

server:
  servlet:
    encoding:
      charset: UTF-8

api:
  url:
    baseUrl: https://48d3-210-100-160-190.jp.ngrok.io